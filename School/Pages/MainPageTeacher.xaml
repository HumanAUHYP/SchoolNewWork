<Page x:Class="School.Pages.MainPageTeacher"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:School.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="#F8F8F8"
     FontFamily="{DynamicResource MaterialDesignFont}"
      Title="MainPageTeacher">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"  Orientation="Vertical" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Кружки" VerticalAlignment="Center" HorizontalAlignment="Center" Click="Button_Click">

                </Button>
                <Button Grid.Column="2" Content="Ученики" VerticalAlignment="Bottom" HorizontalAlignment="Center" Click="Button_Click_1">

                </Button>
                <Button Grid.Column="3" Content="Журнал" VerticalAlignment="Bottom" HorizontalAlignment="Center" Click="Button_Click_2" Cursor="Hand">

                </Button>
                <Button Grid.Column="4" Content="Расписание" VerticalAlignment="Center" HorizontalAlignment="Center" Cursor="Hand" Click="Button_Click_3" >

                </Button>
            </Grid>

            <!--<Border
                VerticalAlignment="Bottom"
                BorderBrush="Black"
                BorderThickness="1" />-->
        </StackPanel>
        <Image Source="/img/лого.png" Grid.Row="1" HorizontalAlignment="Center" Width="572"/>

    </Grid>
</Page>
